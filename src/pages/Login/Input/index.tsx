import { AiOutlineLock, AiOutlineUser } from "react-icons/ai";
import { ContinueButton, Input, LoginText, PasswordDivInput, UserDivInput } from "../styles";


export default function Form() {
    return (
        <>
            {/* <UserDivInput>
                <Input
                    type="text"
                    value={form.user}
                    onChange={(e) => setForm({
                        ...form,
                        user: e.target.value
                    })} />
                <AiOutlineUser size={25} />
            </UserDivInput><PasswordDivInput>
                    <Input
                        type="password"
                        value={form.password}
                        onChange={(e) => setForm({
                            ...form,
                            password: e.target.value
                        })} />
                    <AiOutlineLock size={25} />
                </PasswordDivInput> */}
        </>
    )
}